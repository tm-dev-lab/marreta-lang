# Deps baked so the timed run is the test execution only (the inner-loop feedback).
FROM python:3.12-slim@sha256:a75662dfec8d90bd7161c91050be2e0a9b21d284f3b7a7253d5db25f7d583fb3
WORKDIR /app
RUN pip install --no-cache-dir fastapi pytest httpx motor
COPY app.py test_app.py ./
CMD ["pytest", "-q"]
